version: 2

models:
  - name: fct_liquidity_forecasting__cash_daily_combined
    description: "Daily actual & forecast signed cash deltas by business and scenario."
    columns:
      - name: business_id
        tests: [not_null]
      - name: date_day
        tests: [not_null]
      - name: scenario
        tests: [not_null]